// Runtime client config. Loaded by /signup.html, /login.html, /account.html.
//
// Set window.SANAD_GOOGLE_CLIENT_ID to your Google Identity Services client
// ID to enable Google sign-in. Leave empty to hide the Google button (the
// auth-client falls back gracefully).
//
// In production you can serve this file dynamically from the server with the
// real client ID baked in, or replace the file at deploy time.
window.SANAD_GOOGLE_CLIENT_ID = '';
